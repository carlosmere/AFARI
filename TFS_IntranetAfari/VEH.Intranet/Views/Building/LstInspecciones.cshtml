@model VEH.Intranet.ViewModel.Building.LstInspeccionesViewModel
@{
    ViewBag.Title = "Inspecciones";
    ViewBag.Section = "Edificio";
}

@section Breadcrumbs{
    <li><a href="@Url.Action("LstEdificio", "Building")">Lista de Edificios</a></li>
    <li><a href="@Url.Action("LstOpcionEdificio", "Building", new { EdificioId = Model.EdificioId })">Opciones Edificio</a></li>
}

<div>
    @*class="row">*@
    <div>
        @*class="col-sm-12">*@
        @*<div>
                <div>
                    <div class="panel panel-inverse">
                        <div class="panel-heading">
                            <h4 class="panel-title"><i class="fa fa-filter"></i>&nbsp;&nbsp;Filtro de Preguntas</h4>
                        </div>
                        <div class="panel-body">
                            <form class="form-horizontal">
                                <div class="form-group">
                                    <label class="col-md-2 control-label">Pregunta</label>
                                    <div class="col-md-8">
                                        @Html.DropDownListFor(x => x.PreguntaFiltrado, Model.LstPreguntas, "[-Seleccione-]", new { @class = "form-control" })
                                    </div>
                                </div>
                                <hr />
                                <div class="form-group">
                                    <div class="col-md-2 col-md-offset-10 ui-sortable">
                                        <button type="submit" class="btn btn-sm btn-primary m-r-5"><i class="fa fa-search"></i>&nbsp;&nbsp;Filtrar</button>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>*@
        <div class="panel panel-inverse">
            <form method="post" class="form-horizontal" enctype="multipart/form-data">
                <div class="panel-heading">
                    @*<div class="panel-heading-btn">
                    <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-warning" data-click="panel-collapse" data-original-title="" title="Colapsar / Expandir"><i class="fa fa-minus"></i></a>
                </div>*@
                    <h4 class="panel-title"><i class="fa fa-list"></i>&nbsp;&nbsp;Estadísticas</h4>
                    @*<div class="form-horizontal form-bordered">
                </div>*@
                </div>


                <div class="form-group panel-body">
                    <div class="col-md-6">
                        @Html.LabelFor(x => x.FechaInicio, new { @class = "col-md-4 control-label" })
                        <div class="col-md-8">
                            @Html.DateTextBoxFor(x => x.FechaInicio, new { @class = "form-group" })
                        </div>
                    </div>
                    <div class="col-md-6">
                        @Html.LabelFor(x => x.FechaFin, new { @class = "col-md-4 control-label" })
                        <div class="col-md-8">
                            @Html.DateTextBoxFor(x => x.FechaFin, new { @class = "form-group" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-2 col-md-offset-10 ui-sortable">
                            <button type="submit" class="cancel btn btn-sm btn-primary m-r-5"><i class="fa fa-search"></i>&nbsp;&nbsp;Generar Reporte</button>
                            @*<a href="@Url.Action("DownloadReporteInspeciones", "Building", new { EdificioId = Model.EdificioId, FechaInicio = Model.FechaInicio, FechaFin = Model.FechaFin })" class="btn btn-sm btn-default"><i class="fa fa-file-text"></i>&nbsp;Generar Reporte</a>*@
                        </div>
                    </div>
                </div>
            </form>
                </div>
        <div class="panel panel-inverse">
            <div class="panel-heading">
                <h4 class="panel-title"><i class="fa fa-list"></i>&nbsp;&nbsp;Listado</h4>
            </div>
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th class="col-sm-2 text-center">Nombre</th>
                                <th class="col-sm-4 text-center">Fecha</th>
                                <th class="col-sm-2 text-center">Opciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.LstInspecciones)
                            {
                                <tr>
                                    <td class="text-center">@item.Nombre</td>
                                    <td class="text-center">@item.Fecha.ToShortDateString()</td>
                                    <td class="text-center">
                                        <div class="input-group">
                                            <div class="input-group-btn btn-xs">
                                                <a href="@Url.Action("AddEditInspeccion", "Building", new { InspeccionId = item.InspeccionId })" class="btn btn-default btn-xs"><i class="fa fa-eye"></i>&nbsp;Ver Detalles</a>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        
                @*@foreach (var item in Model.LstHistorialPreguntas)
                {
                    <div class="panel panel-inverse">
                        <div class="panel-heading">
                            <h4 class="panel-title">&nbsp;&nbsp;Pregunta @item.Key</h4>
                        </div>
                        <div class="panel-body">
                            <div class="table-responsive">
                                <table class="table table-hover">
                                    <thead>
                                        <tr>
                                            <th class="col-sm-4 text-center">Fecha</th>
                                            <th class="col-sm-2 text-center">Puntaje</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item2 in item.Value)
                                        {
                                            <tr>
                                                <td class="text-center">@item2.Key.ToShortDateString()</td>
                                                <td class="text-center">@item2.Value</td>
                                            </tr>
                                        }

                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                }*@
                @*<div class="panel-body">
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th class="col-sm-2 text-center">Pregunta</th>
                                        <th class="col-sm-4 text-center">Fecha</th>
                                        <th class="col-sm-2 text-center">Puntaje</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.LstHistorialPreguntas)
                                    {
                                        foreach (var item2 in item.Value)
                                        {
                                            <tr>
                                                <td class="text-center">@item.Key</td>
                                                <td class="text-center">@item2.Key.ToShortDateString()</td>
                                                <td class="text-center">@item2.Value</td>
                                            </tr>
                                        }
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>*@
  
    </div>
</div>
@*<a class="btn btn-sm btn-primary" href="@Url.Action("AddEditInspeccion", "Building")"><i class="fa fa-plus"></i>&nbsp;Agregar inspeccion</a>*@
